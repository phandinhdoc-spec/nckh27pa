# T9 — CommandCode continuation: SOS fail-soft runtime (DeepSeek V4 Pro)

## WORKDIR
`/home/pdd/nckh27pa/android` (repository root `/home/pdd/nckh27pa`)

## SITUATION
This is a CONTINUATION. Do NOT re-investigate the repository from scratch and do NOT re-read whole files
that are not listed here.

Read first (targeted only):
1. `/home/pdd/nckh27pa/.ai/luna-sos-runtime-investigation.md` — prior investigation (do not edit this file).
2. The working-tree diff: `git -C /home/pdd/nckh27pa diff -- android/app/src`
   and the untracked new files listed below (current state, uncommitted).

Current uncommitted state (already reviewed by the PM; treat as the baseline, do not revert):
- modified: `emergency/EmergencyCore.kt`, `emergency/AndroidSmsManagerGateway.kt`, `emergency/EmergencyPersistence.kt`,
  `location/AndroidEmergencyLocationController.kt`, `location/LocationResolution.kt`, `DemoApplication.kt`,
  `permissions/*`, `HomeScreen.kt`, `app/build.gradle.kts`, plus test files.
- new (untracked): `location/LocationRepository.kt`, `location/AndroidPlatformLocationSource.kt`,
  `emergency/AndroidSimCallGateway.kt`, test `emergency/SosCallStepTest.kt`,
  test `location/BestAvailableLocationRepositoryTest.kt`.

Target architecture ALREADY implemented in the baseline (verify, do not redesign):
```
SOS
 ├── location acquisition (async, bounded 8s: cache -> FUSED -> GPS -> NETWORK)
 ├── emergency SMS (independent)
 └── emergency SIM call via Intent.ACTION_CALL (independent)
```
Location failure of any kind must NEVER abort SMS or CALL. Backend failure must never abort local SMS/CALL.
Late location after the first SMS must produce AT MOST ONE supplemental SMS containing the Maps link.

## YOUR TASK (exactly these four things)

### 1. Review the existing diff for real defects
Look specifically for: compile errors, coroutine/lifecycle bugs, races, duplicate actions,
any remaining path where location blocks SMS/CALL, and tests that pass while production wiring is wrong.
Do NOT restyle or refactor code that is already correct.

### 2. Fix defect A — supplemental SMS fires even when the first SMS already carried a location
`EmergencyCore.kt` → `EmergencyCoordinator.dispatch` / `updateLocation`.
Today `updateLocation` sends a supplemental SMS for every dispatched event once any fix arrives, even when the
initial SMS already contained coordinates. Requirement: the supplemental SMS exists only for the case
"first SMS had NO location, location arrived later".
Required: record whether the dispatch already had a fix (e.g. a new `var dispatchedWithFix: Boolean = false`
field on `EmergencyRecord`, set in `dispatch()`; keep it Gson-compatible with a default so already-persisted
records still deserialize) and gate the supplement on it. Keep: at most one supplement per contact per event,
no supplement when cancelled, no supplement when MESSAGING permission is missing.

### 3. Fix defect B — blank/invalid phone numbers are still handed to the SMS gateway
`EmergencyCore.kt` → `EmergencyCoordinator.dispatchSms`.
Today a contact with a blank phone reaches `EmergencySmsGateway.send` (the Android gateway then fails at the
radio call). Required: skip contacts with a blank phone per-contact, count them as not-queued, and surface the
fact in the step detail. Do NOT drop numbers that fail the strict Vietnamese regex
(`ContactValidator.validate`) — international numbers, short codes and other valid destinations must still be
sent; only clearly empty/blank numbers are skipped. Never hard-code a phone number anywhere.
`ContactValidator.normalize` is already applied to `record.contacts` in `dispatch()` — do not duplicate it.

### 4. Add the missing regression tests (JUnit4 + kotlinx-coroutines-test only; no Robolectric in this project)
In `app/src/test/java/vn/nckh27pa/fallsafe/emergency/SosCallStepTest.kt` (or a new test file in the same package):
- location PERMISSION_DENIED (`BestAvailableLocationRepository` + a `PlatformLocationSource` whose
  `permission()` is `LocationPermission.DENIED`) → SMS still queued AND SIM call still attempted.
- location TIMEOUT / UNAVAILABLE → SMS still queued AND SIM call still attempted, message contains no
  fabricated coordinates and no `maps.google` link.
- NO supplement when the first SMS already contained a fix: dispatch with a valid fix, then call
  `updateLocation` twice with another valid fix → exactly ONE send total (the initial one).
- supplement is still exactly one when the first SMS had no fix and several different fixes arrive.
- a contact with a blank phone does not produce a second radio call but must not stop other contacts.

## RULES
- Kotlin/Android only. Minimal, targeted patches. Prefer `patch`/small edits over rewriting whole files.
- Do NOT commit, do NOT push, do NOT create branches.
- Do NOT run `git checkout`, `git restore`, `git stash`, `git reset` — the working tree changes are the user's work.
- Do NOT edit `docs/`, `.ai/luna-sos-runtime-investigation.md`, UI layout, `HomeScreen.kt`, `MainActivity.kt`,
  `AndroidManifest.xml`, `app/build.gradle.kts`.
- Do NOT change the current CALL-vs-backend policy. Note: `docs/decisions.md` D09 says the SIM call should be
  SKIPPED when the backend voice adapter reports STARTED, while the current code and tests always place the
  handset call. This contradiction is a known owner decision pending — leave both as they are and report it.
  Do not "fix" it in either direction.
- Do NOT add new dependencies.

## VALIDATION (mandatory, report raw output)
Java 25 breaks Gradle in this environment. Always:
```
cd /home/pdd/nckh27pa/android && JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 ./gradlew testDebugUnitTest --console=plain
```
must be GREEN (0 failures, 0 errors) before:
```
cd /home/pdd/nckh27pa/android && JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 ./gradlew assembleDebug --console=plain
```
Then report the totals parsed from `app/build/test-results/testDebugUnitTest/TEST-*.xml`.
If a test fails, use the compiler/test output plus `rg` + `sed` to fix only the offending file/line.
If you fail twice on the SAME error, STOP and report it unresolved instead of guessing further.

## OUTPUT (last lines of your answer, exactly this shape)
MODEL: deepseek/deepseek-v4-pro
REVIEW FINDINGS:
CHANGED FILES:
TEST RESULT (tests/failures/errors/skips):
BUILD RESULT:
UNRESOLVED:
NEXT:
