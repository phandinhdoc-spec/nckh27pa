# FIX-AND-001B — Sửa 2 lỗi review trước demo
Nguồn REV-AND-001B.txt (P2 replay pause, invalid sample không reset). Mục tiêu tái hiện RED ở seam tích hợp rồi sửa tối thiểu. Người Codex gpt-6-astra phiên fix riêng. Đọc review + app source/tests; không cả kế hoạch.
Phạm vi sửa android/app/src/ và android/evidence/FIX-AND-001B-*, android/README.md nếu cần. Không core/Gradle/docs/plans. Phụ thuộc AND-001B + review FAIL. Hermes chưa nghiệm thu dù build pass.
Bàn giao: regression test bắt onPause làm replay mất impact; test đi qua normalizer → adapter → session với NaN giữa quiet window, không bypass normalizer như test cũ. Tách seam pure Kotlin nếu cần để controller/collector gọi đúng seam được test. TDD RED trước fix, GREEN + suite. App real invalid accel cần clear detection; invalid optional không phá accel vô cớ. onPause clear real data/detector nếu PHONE_ONLY, không reset replay; deadline VERIFYING vẫn tồn tại.
Nghiệm thu cả2 lỗi không tái hiện, source still scoped, no actual alerts. Hermes chạy Gradle/emulator; agent chỉ Kotlin/JUnit offline như runner cũ. Không build Gradle/network/agent con, không sửa test để bỏ requirement.2 cycles tối đa; read-only reviewer riêng sau fix.
