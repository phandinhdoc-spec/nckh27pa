# DEC-001 — Decoder sensor v1 trên Android
Mục tiêu: giải mã JSON Esp32SensorPacket đúng schema/đơn vị từ contract, chạy fixtures thật thay vì chỉ lưu checklist. Chưa transport BLE/hardware, không dùng decoder làm detector.
Nguồn Android §6.1/6.3/6.8/8; docs/fixtures/protocol-v1.json. Người Hermes; review AGY inline độc lập nếu còn quota.
Input contract sensor + fixtures8; scope android/app/src/{main,test}/.../protocol, app/build.gradle.kts dependency Gson2.11.0 và fixture test resources. Không đổi UUID/API/plans/core.
Phụ thuộc ENV-002 build working. Bàn giao data class đúng tên, Esp32PacketDecoder.decodeSensor, typed rejects malformed/missing/duplicate JSON keys/wrongversion/requirednull/range/overlength/NaN; nullable optional absent/explicit null theo ghi chú chính sách parser. Không dedupe state ở decoder thuần: caller dedupe (deviceId,sequenceNumber).
Nghiệm thu Gradle tests/build pass, fixtures accept/reject đúng; decode lỗi không crash app/caller nhận null. JSON bounded4096char, deviceId1..128, numbers finite, timestamp/seq nonnegative Long. Bound kỹ thuật prototype, không tự thay wire version.
Kiểm: tests trước implementation, log RED/GREEN; reviewer độc lập;2 lần không tiến triển dừng hướng. Không network cảnh báo hoặc thêm full backend. Những lớp packet khác và ACK cache thuộc task sau theo contract.
