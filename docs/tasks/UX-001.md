# UX-001 — Phản biện trạng thái giao diện độc lập
Mục tiêu: checklist nghiệm thu UI lát PHONE_ONLY, xác minh và cảnh báo thử nghiệm; không triển khai trùng tệp AND-001.
Nguồn: android/android-plan.md §3, §4.4, §4.5.5, §7, §8, §10.
Thực hiện: AGY CLI gemini-3.8-flash-low (model do agy models trả về; lần gọi này kiểm chứng quyền thực).
Đầu vào: các mục nguồn và docs/tasks/AND-001.md; không đọc toàn bộ repo hay secrets.
Phạm vi sửa: không sửa tệp; chỉ trả kết quả. Hermes lưu đầu ra và tích hợp sau khi kiểm tra.
Phụ thuộc: khảo sát; độc lập AND-001.
Bàn giao: checklist ngắn bằng tiếng Việt, trạng thái thiếu cảm biến/quyền/vị trí/BLE; hạn xác minh, SAFE, SOS, không phản hồi, lỗi gửi; phân biệt demo/đã gửi/đã nhận. Phát hiện tối đa5 thiếu sót rủi ro lớn và trích mục kế hoạch.
Nghiệm thu: bao phủ trạng thái trên, không yêu cầu bấm mới gửi, không thêm backend lớn, không gọi thiết bị chẩn đoán y khoa.
Kiểm chứng: Hermes đối chiếu kế hoạch và app sau build. Giới hạn:1 lần gọi, tối đa2 lần nếu có lỗi khác sửa được; không tự đổi model/khởi agent/build/emulator.
