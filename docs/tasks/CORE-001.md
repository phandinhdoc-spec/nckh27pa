# CORE-001 — Lõi cảnh báo Kotlin chạy ngoại tuyến
Mục tiêu: triển khai lõi Kotlin tái sử dụng cho AND-001, không cần Gradle/network; không coi là hoàn thành AND-001/APK.
Nguồn: Android §4.4, §7, §9 (NO_RESPONSE/SAFE/NEED_HELP; trạng thái), yêu cầu người dùng cảnh báo không cần phản hồi. Đọc chỉ các mục này và phiếu, không đọc lại toàn repo.
Người: Codex CLI gpt-6-astra đã thực sự gọi được (header docs/evidence/AND-001-codex.log). Không đổi model vì blocker là sandbox DNS không phải reasoning.
Đầu vào: Kotlin local /home/pdd/.local/opt/nckh27pa/android-studio/plugins/Kotlin/kotlinc/bin/kotlinc; JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64. Xác minh chạy compiler trước. Không network. Không có source trước.
Phạm vi sửa: android/core/ (source, tests, run-tests.sh, README, evidence). Không file khác, không sửa kế hoạch, không commit hoặc subprocess agent. Phụ thuộc SUR-001; AND-001 hạ tầng bị chặn, CORE không phụ thuộc hạ tầng mạng.
Bàn giao: source Kotlin không Android dependency, test harness check/assert, executable test runner tái lập và log RED/GREEN từng hành vi. Có thể dùng kotlinc compile jar + java -jar. Lõi gồm state machine tách UI, monotonic clock injection, test sink/recorded events bounded, snapshot hiển thị. Không triển khai thuật toán ngã tưởng tượng: suspected và evidenceConfirmed đầu vào do detection tương lai phát. CLI demo chuyển MONITORING→SUSPECTED→VERIFYING→ALERTING→AWAITING_HELP với đồng hồ giả và người nhận giả; rõ chỉ core, không sensor/UI thật.
Nghiệm thu: mỗi hành vi TDD RED rồi GREEN, final suite exit0. Không phản hồi tại deadline10s tự gửi test sink đúng1lần; SAFE trước deadline hủy nhưng tại/sau deadline không xóa cảnh báo; NEED_HELP/SOS ngay; nhận SOS khi đang cần trợ giúp không spam; complete event quay monitoring và SOS mới được; callback trùng/repeated suspect không kéo dài hạn; thiếu vị trí không chặn; sink failure không SENT; wall time không điều khiển deadline. Test boundary và invalid config (timeout<=0), bounded log nếu có. Thread ownership/concurrency contract rõ, state mutation tránh reentrant sink tạo gửi trùng. Không raw phone/contact/location logs.
Kiểm chứng: run-tests.sh và demo, Hermes chạy lại, reviewer phiên riêng trước tích hợp. Retry tối đa2 lần không tiến triển mỗi hướng. Nếu compiler bị chặn quyền dừng không bypass. Mục này là lõi thử nghiệm, không tuyên bố giải quyết background/persistence/real transport.
