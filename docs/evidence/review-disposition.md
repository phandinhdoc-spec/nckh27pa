# Hermes xử lý findings
- REV-CORE-002 PASS, P3 assertion bị catch trong failing sink: chuyển assert ra ngoài callback, giữ regression sau triển khai; suite12 nhóm chạy lại PASS.
- REV-AND-001B FAIL2P2: invalid ACCEL không reset, pause reset replay. Codex dừng vì quota sau RED. Hermes chạy lại RED2fails/13, sửa adapter và kiểm wiring actual, GREEN13; Gradle build/lint pass. AGY narrow review PASS2fix; câu “Core remaining deadline không reset vẫn là lỗi” là hiểu nhầm requirement, không có repro: deadline KHÔNG reset khi pause là hành vi đúng, test xác nhận. Không dùng câu đó làm bug mới.
- AGY Android full review timeout sau4m chỉ output partial, không phải full PASS. Narrow review là lần đổi cách sau timeout, phạm vi rõ. Review gốc Codex không tìm blocker ngoài2P2.
- AGY protocol PASS static: loại cách diễn đạt “an toàn tuyệt đối/không leak” và “footprint4KB–6KB” thành hạn chế: fixed slots nhưng Result/encoder/JVM có allocation; Kotlin heap total chưa đo. Chỉ host sanitizer/tests của Hermes là bằng chứng execution. Header/generation64bit có miền chung giới hạn, lab không wire generation; không tuyên bố tương thích mọi integer ngoài miền chung.
- ESP reviewer PASS, P3 thiếu action mask timeout+SAFE/CANCEL và WrongId tại timeout: bổ sung regression theo đề xuất trước đóng task; không gọi test mới là test-first.
- Smoke emulator lần đầu5/6 PASS; ca sink-failure thất bại vì harness bấm Text label không phải Switch. XML có hai node cùng tên, đã ưu tiên clickable/content-desc; rerun riêng sink-failure PASS. Giữ log đầu, không sửa app để né test. Chạy lại full trên APK cuối.
