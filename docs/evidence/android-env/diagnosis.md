# ENV-002: chẩn đoán theo ranh giới thực thi
Quan sát gốc: sandbox workspace-write Codex curl hai domain Gradle exit6 Could not resolve host (android/evidence/...). Lệnh ./gradlew exit127 vì chưa có Wrapper — hậu quả thiếu project bootstrap, không phải compiler error. Không sửa sandbox.
Probe host terminal Hermes hiện tại: getent ahosts downloads.gradle.org, dl.google.com trả địa chỉ; curl -IL tới Gradle official theo redirect nhận200, Google Maven AGP8.13.0 POM200, Maven Central Kotlin2.2.10 POM200 (host-network.log). Vì vậy không có bằng chứng lỗi DNS toàn máy trong lần đo này; khả năng mạng sandbox bị hạn chế là ranh giới quan sát được, chưa khẳng định cấu hình kernel cụ thể nếu không đo namespace.
Host tải Gradle8.13 zip thật, SHA256 so endpoint chính thức =20f1b1176237254a6fc204d8434196fa11a4cfb387567519c61556e8710aed78. Sinh wrapper bằng task wrapper của Gradle này; distributionSha256Sum đã pin. JAVA_HOME riêng lệnh=/usr/lib/jvm/java-21-openjdk-amd64,21.0.12; không đổi JDK25 global. SDK platforms36/build-tools36.0.0 đã có; dùng buildToolsVersion explicit để không tự đòi35.
Wrapper :app:dependencies --configuration debugRuntimeClasspath thành công (dependencies.log); Gradle8.13, AGP8.13.0, Kotlin2.2.10, Compose BOM2025.08.01. Không đồng nghĩa đã APK; build khi source bàn giao. Tham khảo compatibility: https://developer.android.com/build/releases/agp-8-13-0-release-notes và https://docs.gradle.org/8.13/release-notes.html .
Người dùng cho phép Hermes terminal build bình thường. Không danger-full-access, không đổi security policy hoặc đưa proxy lách sandbox vào agent. Các lệnh lỗi và thành công giữ riêng; không xóa log cũ.
