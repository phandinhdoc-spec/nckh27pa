# Runtime RED: permission callback before onResume
Emulator API36, APK AND-002A-build-3. Steps: fresh process → Cài đặt → Bật giám sát nền thử → hệ thống hỏi notification → Allow. Sau1s UI vẫn “Giám sát nền chưa bật”; dumpsys activity services package=(nothing). Evidence background/permission-prompt.xml, started.xml, service-started.txt. Activity was paused by system dialog; onRequestPermissionsResult foreground flag false skipped start. Fix: defer start until next onResume, not start in background and not require second user tap. Rerun with revoked notification permission before test.
