DOC-CLOSE-001. Chỉ trả nội dung tiếng Việt tối đa220từ; KHÔNG tool/read_file/network/agent con. Bạn là worker duy nhất viết bản đóng mốc, không sửa mã. Viết3đoạn: kết quả; chưa kiểm chứng/còn lại; NEXT_ACTION. Không nói tất cả việc còn lại bị chặn, không thêm số liệu.
Dữ kiện đã được Hermes kiểm chứng ở docs/evidence/final-verification.json: Android Gradle Wrapper build/lint thành công, 21 JUnit tests passed, 7 lint warnings không errors. Core Kotlin riêng12nhóm PASS. ESP C++ host11nhóm+sanitizer/demo PASS, chưa Arduino build đúngbo. Framing lab52scenarios mỗi ngôn ngữ Kotlin/C++ +56cross-language PASS, sanitizer host PASS; chưa approved/live BLE. Emulator smoke6nhóm, background2nhóm, SensorManager+scroll1nhóm đều PASS trên APK cuối. APK /home/pdd/nckh27pa/android/app/build/outputs/apk/debug/app-debug.apk. Hai kế hoạch và setup script không đổi theo SHA256.
Chưa điện thoại/ESP thật, real BLE/recipients, độ chính xác ngã/pin/OEMDoze/processdeath recovery; persist/location/bước/pin/âm-rung còn backlog. Không gọi emulator là phần cứng thật. Không thay các kết quả cũ khi đổi chính sách.
Chính sách mới: Hermes chỉ PM, một worker mỗi task, tối đa một reviewer khi cần. Routine/docs/testgen: AGY Gemini default; multi-file Android/ESP/buildfix: Codex; reviewhard/safety: AGY Claude; Level4 chỉ escalate có lý do. Codex từng usage limit, chưa tái kiểm quota nên không khẳng định quota hiện tại. Giá model chưa quan sát. NEXT_ACTION là cập nhật hồ sơ và chọn một task sẵn sàng với đủ STATUS/OWNER/FILES/GOAL/RESULT/NEXT_ACTION theo routing; không tự triển khai mã trước khi giao worker.
